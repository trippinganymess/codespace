#include <cs50.h>
#include <stdio.h>
int main(){
int k = gtllms();
printf("the current population of llamas is %i" k);
}
int gtllms(){
    int n = get_int("what is the current population of llamas");
}